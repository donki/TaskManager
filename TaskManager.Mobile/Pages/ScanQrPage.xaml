<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:loc="clr-namespace:TaskManager.Mobile.Localization"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             x:Class="TaskManager.Mobile.Pages.ScanQrPage"
             Title="{loc:T ScanTitle}">

    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="ic_close_w.png" Clicked="OnCloseClicked" />
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="*,Auto">

        <!-- La camara ocupa todo lo que hay: apuntar a un QR pequeño con una ventanita es justo lo
             que hace que no lo pille. -->
        <zxing:CameraBarcodeReaderView x:Name="Camara" Grid.Row="0"
                                       BarcodesDetected="OnBarcodesDetected" />

        <Label Grid.Row="1" Text="{loc:T ScanHint}" Style="{StaticResource HintText}"
               HorizontalTextAlignment="Center" Padding="20,12" />
    </Grid>
</ContentPage>
