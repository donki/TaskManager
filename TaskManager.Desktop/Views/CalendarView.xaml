<UserControl x:Class="TaskManager.Desktop.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:TaskManager.Desktop.Localization">

    <Grid ColumnDefinitions="*,260">

        <!-- Mes -->
        <Grid Grid.Column="0" RowDefinitions="Auto,Auto,*" Margin="0,0,16,0">

            <!--
                Los tres botones van con el estilo de icono del resto de la aplicacion. Con el
                estilo de fabrica de WPF salian como cajas blancas: en el tema oscuro se veian tres
                rectangulos claros y el glifo casi no se leia.
            -->
            <Grid Grid.Row="0" ColumnDefinitions="Auto,Auto,*,Auto" Margin="0,0,0,10">
                <Button Grid.Column="0" Style="{StaticResource GhostIconButton}" Content="&#xE76B;"
                        ToolTip="{loc:T PreviousMonth}" Click="OnPreviousMonthClick" />

                <!-- Volver a hoy: navegando cuatro meses hacia delante, regresar a mano es contar
                     hacia atras, y el mes de hoy es al que siempre se quiere volver. -->
                <Button Grid.Column="1" Style="{StaticResource GhostIconButton}" Content="&#xE787;"
                        Margin="6,0,0,0"
                        ToolTip="{loc:T CalendarToday}" Click="OnTodayClick" />

                <TextBlock Grid.Column="2" x:Name="MonthLabel" FontSize="17" FontWeight="SemiBold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextPrimary}" />

                <Button Grid.Column="3" Style="{StaticResource GhostIconButton}" Content="&#xE76C;"
                        ToolTip="{loc:T NextMonth}" Click="OnNextMonthClick" />
            </Grid>

            <UniformGrid Grid.Row="1" x:Name="WeekdayRow" Columns="7" Margin="0,0,0,4" />

            <UniformGrid Grid.Row="2" x:Name="MonthGrid" Columns="7" Rows="6" />
        </Grid>

        <!-- Tareas del dia elegido -->
        <Grid Grid.Column="1" RowDefinitions="Auto,*">
            <TextBlock Grid.Row="0" x:Name="DayLabel" FontSize="14" FontWeight="SemiBold"
                       Margin="0,0,0,8" TextWrapping="Wrap"
                       Foreground="{DynamicResource TextPrimary}" />

            <ListBox Grid.Row="1" x:Name="DayTasks"
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     HorizontalContentAlignment="Stretch"
                     MouseDoubleClick="OnDayTaskDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,6" />
                        <Setter Property="Background" Value="Transparent" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBackground}" CornerRadius="8" Padding="10,8">
                            <StackPanel Opacity="{Binding Opacity}">
                                <TextBlock Text="{Binding Title}" TextWrapping="Wrap" FontSize="13"
                                           TextDecorations="{Binding Decoration}"
                                           Foreground="{DynamicResource TextPrimary}" />
                                <TextBlock Text="{Binding ListName}" FontSize="11"
                                           Foreground="{DynamicResource TextSecondary}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Grid.Row="1" x:Name="EmptyLabel" FontSize="12" TextWrapping="Wrap"
                       VerticalAlignment="Top" Margin="4,10,0,0"
                       Foreground="{DynamicResource TextSecondary}" />
        </Grid>
    </Grid>
</UserControl>
